# Google Cloud Build configuration for Cloud Run deployment
steps:
  # Pull cache image (ignore if not exists)
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args: ['-c', 'docker pull gcr.io/$PROJECT_ID/campone-dashboard:latest || exit 0']

  # Build the Docker image with cache
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--cache-from'
      - 'gcr.io/$PROJECT_ID/campone-dashboard:latest'
      - '-t'
      - 'gcr.io/$PROJECT_ID/campone-dashboard:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/campone-dashboard:latest'
      - '.'

  # Push the Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/campone-dashboard:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/campone-dashboard:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'campone-dashboard'
      - '--image'
      - 'gcr.io/$PROJECT_ID/campone-dashboard:$COMMIT_SHA'
      - '--region'
      - 'asia-northeast3'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '1Gi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--add-cloudsql-instances'
      - 'campone-v1-0:asia-northeast3:campone'
      - '--set-env-vars'
      - 'NEXTAUTH_URL=https://campone-dashboard-755458598444.asia-northeast3.run.app'
      - '--set-secrets'
      - 'NEXTAUTH_SECRET=campone-admin-secret:latest,DATABASE_URL=DATABASE_URL_DASHBOARD:latest,EMBED_JWT_SECRET=embed-jwt-secret:latest,INTERNAL_API_KEY=dashboard-api-key:latest'

images:
  - 'gcr.io/$PROJECT_ID/campone-dashboard:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/campone-dashboard:latest'

options:
  logging: CLOUD_LOGGING_ONLY
